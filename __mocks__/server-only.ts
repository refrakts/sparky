// Empty stub for the `server-only` package, used in vitest.config.ts via
// resolve.alias. The real package throws at import time outside a Next.js
// build to enforce server boundaries; tests need to import server modules.
export {};
